# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Tourist Helper' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  #Adding App Center Analytics for overall usage of the app.
  pod  'AppCenter'
  
  # Pods for Tourist Helper
  
  # Adding pod file for Push SDK
  pod 'AppCenter/Push'

  target 'Tourist HelperTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Tourist HelperUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
